{
  "name": "fpga-js-emulator32",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@craco/craco": "^5.6.2",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-modal": "^3.11.2",
    "react-scripts": "3.2.0",
    "react-window": "^1.8.5"
  },
  "scripts": {
    "start": "craco start",
    "build": "craco build",
    "test": "craco test",
    "eject": "react-scripts eject",
    "size": "size-limit"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "size-limit": [
    {
      "path": "src/App.js"
    }
  ],
  "devDependencies": {
    "@size-limit/preset-app": "^2.1.6",
    "size-limit": "^2.1.6"
  }
}
